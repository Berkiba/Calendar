name: calender
services:
  web:
    image: web.calender
    build:
      context: ../
      dockerfile: .docker/dockerfile
    container_name: web.calender
    restart: always
    ports:
      - "80:5173"
    volumes:
      - ../:/website:ro
